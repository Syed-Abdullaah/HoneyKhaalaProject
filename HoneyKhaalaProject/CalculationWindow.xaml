<Window x:Class="HoneyKhaalaProject.CalculationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:vm="clr-namespace:HoneyKhaalaProject.VM;assembly=HoneyKhaalaProject.VM"
        mc:Ignorable="d"
        Title="Investment Calculation" Height="680" Width="620"
        Background="#F3F6FA" WindowStartupLocation="CenterOwner"
        d:DataContext="{d:DesignInstance Type=vm:MainVM,IsDesignTimeCreatable=True}"
		>
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="28" VerticalAlignment="Top" HorizontalAlignment="Center" Width="560">

                <Border CornerRadius="14" Padding="12" Margin="0,0,0,14" Background="#0F172A">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse Width="48" Height="48" Fill="White" VerticalAlignment="Center"/>
                        <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Calculate Shares" FontSize="18" FontWeight="Bold" Foreground="White"/>
                            <TextBlock Text="Enter investors and click Save Snapshot to persist this month's data" FontSize="12" Foreground="#A8B0C0"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Background="White" CornerRadius="12" Padding="14" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Investors" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>
                        <ItemsControl x:Name="InvestorsList" ItemsSource="{Binding Investors}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <DockPanel Margin="0,6,0,6">
                                        <StackPanel Width="220" DockPanel.Dock="Left">
                                            <TextBlock Text="Name" FontSize="11" Foreground="#6B7280"/>
                                            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0" Height="28" />
                                        </StackPanel>

                                        <StackPanel Width="180" Margin="12,0,0,0" DockPanel.Dock="Left">
                                            <TextBlock Text="Amount (USD)" FontSize="11" Foreground="#6B7280"/>
                                            <TextBox Text="{Binding AmountString, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0" Height="28"/>
                                        </StackPanel>

                                        <Button Content="Remove" Width="70" HorizontalAlignment="Right" Click="RemoveInvestor_Click" Tag="{Binding}" />
                                    </DockPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,8,0,0">
                            <Button Content="+ Add Investor" Width="140" Click="AddInvestor_Click" Margin="0,0,8,0"/>
                            <Button Content="Calculate" Width="140" Click="Calculate_Click" Background="#2563EB" Foreground="White"/>
                            
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Background="White" CornerRadius="12" Padding="14">
                    <StackPanel>
                        <DockPanel>
                            <TextBlock Text="Results" FontSize="16" FontWeight="Bold" DockPanel.Dock="Left"/>
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                                <TextBlock Text="Total: " Foreground="#6B7280" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="{Binding TotalDisplay}" DataContext="{Binding }" FontWeight="Bold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DockPanel>

                        <ItemsControl ItemsSource="{Binding Investors}" Margin="0,10,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,8,0,8">
                                        <DockPanel>
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" Width="200"/>
                                            <TextBlock Text="{Binding DisplayAmount}" Width="120" HorizontalAlignment="Right" Foreground="#111827"/>
                                            <TextBlock Text="{Binding DisplayPercentage}" Width="80" HorizontalAlignment="Right" Foreground="#6B7280"/>
                                        </DockPanel>

                                        <Grid Margin="0,6,0,0" Height="26">
                                            <Border Background="#F1F5F9" CornerRadius="6" Height="26"/>
                                            <Rectangle Height="26" RadiusX="6" RadiusY="6" Width="{Binding BarWidth}" HorizontalAlignment="Left">
                                                <Rectangle.Fill>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                        <GradientStop Offset="0" Color="#4F46E5"/>
                                                        <GradientStop Offset="1" Color="#06B6D4"/>
                                                    </LinearGradientBrush>
                                                </Rectangle.Fill>
                                            </Rectangle>
                                            <TextBlock Text="{Binding DisplayPercentage}" Foreground="White" FontWeight="Bold" HorizontalAlignment="Right" Margin="8,0,8,0" VerticalAlignment="Center"/>
                                        </Grid>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>